services:
  app:
    build:
      context: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3006:3000"
    environment:
      DATA_DIR: /data
      PORT: 3000
      VINEXT_TRUST_PROXY: "1"
    volumes:
      - forma_data:/data

networks: {}

volumes:
  forma_data:
